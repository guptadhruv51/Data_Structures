var message;
message="Hello Geeks!"
console.log(message);
let text; // Creating a variable or declarng a variable
text="JS";
console.log(text);
let mname="Dhruv";
console.log(mname);
const year=2024; 
/**
 * constants need to be intitialised when declared 
 * Const: cannot be intialised again 
 * cannot redeclare let 
 * let message="Hello"
 * let message="Hi"
 * Above thing not allowed
 * 
 * Dont use var these days because it allows to redeclare the variable 
 */
console.log(year);